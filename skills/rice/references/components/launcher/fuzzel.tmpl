# Generated by hypr-rice — merge into ~/.config/fuzzel/fuzzel.ini (note: RRGGBBAA, no '#').
# 11 keys match fuzzel.ini(5) — every documented color slot. Maps:
#   prompt      → {{accent}}  (the `>` prefix; signal color)
#   placeholder → {{muted}}   (de-emphasized hint text)
#   input       → {{fg}}      (the user's typed query)
#   counter     → {{muted}}   (the i/n results counter; subtle info)
[colors]
background={{bg}}f2
text={{fg}}ff
input={{fg}}ff
prompt={{accent}}ff
placeholder={{muted}}ff
match={{accent}}ff
selection={{accent}}ff
selection-text={{bg}}ff
selection-match={{accent2}}ff
counter={{muted}}ff
border={{accent}}ff
