# ----------------------------------------------------------------------------
# Hyprland config — generated by the hyprland-config plugin
# Target version: 0.54.x
# Edit the sourced files below; this file just wires them together.
# ----------------------------------------------------------------------------

# Variables ($mainMod matches the official default + tutorials)
$mainMod     = SUPER
$terminal    = kitty
$menu        = wofi --show drun   # app launcher
$dmenu       = wofi --dmenu       # dmenu mode for piping lists (NOT `$menu --dmenu`)
$browser     = firefox
$fileManager = thunar

# Sourced modules (order matters: env first, then colors so $accent/$bg exist before looknfeel)
source = ~/.config/hypr/env.conf
source = ~/.config/hypr/colors.conf
source = ~/.config/hypr/monitors.conf
source = ~/.config/hypr/input.conf
source = ~/.config/hypr/looknfeel.conf
source = ~/.config/hypr/binds.conf
source = ~/.config/hypr/windowrules.conf
source = ~/.config/hypr/autostart.conf
