# Generated by hypr-rice — merge into ~/.config/starship.toml. Set `palette = "rice"` at the
# top level, then this block defines it. starship reads it live on the next prompt.
palette = "rice"

[palettes.rice]
bg = "#{{bg}}"
fg = "#{{fg}}"
surface = "#{{surface}}"
muted = "#{{muted}}"
accent = "#{{accent}}"
red = "#{{red}}"
green = "#{{green}}"
yellow = "#{{yellow}}"
blue = "#{{blue}}"
magenta = "#{{magenta}}"
cyan = "#{{cyan}}"
