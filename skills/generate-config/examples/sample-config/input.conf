input {
    kb_layout = us
    kb_variant =
    kb_options =
    follow_mouse = 1
    sensitivity = 0

    touchpad {
        natural_scroll = true
        disable_while_typing = true
        tap-to-click = true
        clickfinger_behavior = true
    }
}

# Touchpad gestures — 0.45+ keyword API (drop on desktops / older targets)
gesture = 3, horizontal, workspace
