# Window rules — 0.53+ block form (match: selectors + property = value)

# Generally desirable defaults (from the shipped config)
windowrule {
    name = suppress-maximize
    match:class = .*
    suppress_event = maximize
}
windowrule {
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false
    no_focus = true
}

# Float common utility dialogs
windowrule {
    name = float-utilities
    match:class = ^(pavucontrol|nm-connection-editor|blueman-manager)$
    float = yes
}

# Picture-in-picture: float + pin
windowrule {
    name = pip
    match:title = ^(Picture-in-Picture)$
    float = yes
    pin = yes
}

# Idle inhibit while fullscreen (video/games)
windowrule {
    name = idleinhibit-fullscreen
    match:fullscreen = true
    idle_inhibit = fullscreen
}

# Layer blur for the bar and launcher
layerrule = blur, waybar
layerrule = blur, wofi
